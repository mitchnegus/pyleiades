# Note that tests should be run from test parent directory (package top)
import os, sys                                                                  
sys.path.append(os.path.join(os.path.dirname(os.path.realpath(__file__)), os.pardir))
